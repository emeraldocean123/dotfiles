# Normalize line endings committed to Git
* text=auto eol=lf

# Keep Windows PowerShell profile CRLF in working tree on Windows while storing LF
powershell/Microsoft.PowerShell_profile.ps1 text eol=crlf

# Shell scripts should stay LF
*.sh text eol=lf

# Other PowerShell scripts follow the default (LF in repo); Windows editors handle CRLF automatically
